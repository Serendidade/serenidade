import { createIconSet } from 'react-native-vector-icons'

const glyMaph = require('../.././remixicon.glyphmap.json')

const Icon = createIconSet(glyMaph, 'remixIcon', 'remixicon.ttf')

export default Icon
